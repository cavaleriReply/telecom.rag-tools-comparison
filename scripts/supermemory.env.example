# Template per scripts/supermemory.env (gitignored).
# Serve SOLO per override locali opzionali: le credenziali Vertex (embedder +
# estrazione) vengono lette dal .env del progetto, non servono qui.
# Puoi anche non creare affatto scripts/supermemory.env.

# SHIM_PORT=6799
# SUPERMEMORY_PORT=6767
# SUPERMEMORY_DATA_DIR=/percorso/alternativo
